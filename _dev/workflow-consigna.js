export const meta = {
  name: 'consigna-implementacion',
  description: 'Implementa La Consigna (F.A.R.O): núcleo puro y SQL en paralelo, pantalla, sonda y documentación, revisión adversarial por lentes y arreglo',
  phases: [
    { title: 'Núcleo', detail: 'vocabulario, moldes, armado, lector, repaso, nube · y el SQL' },
    { title: 'Pantalla', detail: 'compositor, anaquel, hojas, CSS y cableado' },
    { title: 'Sonda y documentación', detail: 'la sonda hasta APRUEBA · CLAUDE.md y el plan' },
    { title: 'Revisión', detail: 'cinco lentes adversariales' },
    { title: 'Arreglo', detail: 'cerrar los hallazgos y volver a correr todo' },
  ],
}

const REPO = '/home/user/misiones-autodidacta-familia-polanco-castellanos'
const SPEC = '/tmp/claude-0/-home-user-misiones-autodidacta-familia-polanco-castellanos/c6c9960e-f515-5c83-bbcb-2fce2a1dcb6c/scratchpad/espec-v2.md'
const RUNNER = '/tmp/claude-0/-home-user-misiones-autodidacta-familia-polanco-castellanos/c6c9960e-f515-5c83-bbcb-2fce2a1dcb6c/scratchpad/corre-sonda.js'

const HECHOS = `
HECHOS DEL REPOSITORIO (comprobados; no son opciones):
- Repositorio: ${REPO}. Trabajas directamente en él, en la rama que ya está puesta. NO hagas commit, NO hagas push, NO abras pull requests: eso lo hace el orquestador al final. No toques archivos fuera de tu lista.
- LA ESPECIFICACIÓN COMPLETA está en ${SPEC} (711 líneas, Markdown). Léela ENTERA con Read antes de escribir nada (en dos o tres lecturas con offset). Se implementa tal cual, salvo los RECORTES de abajo.
- RECORTES del orquestador (mandan sobre la especificación): (1) «☑ Elegir» (selección múltiple, barra sticky, mover varios de golpe) NO va en esta versión: la barra del anaquel es [＋ Nueva] · [📋 Pegar] · | · [🗂 Estantes] · [⇅ Orden] · | · [📋 Exportar] · y al final las vistas ▦ ☰; la sonda 18 mide que el ÚLTIMO botón de la barra queda dentro del borde en vez de «☑ Elegir». (2) Todo lo demás de la especificación va. Donde la especificación calle, se copia el patrón de 📓 Cuadernos (js/tools/redaccion-cuadernos.js y css/redaccion-cuadernos.css), que es la herramienta más reciente de la casa y la que el autor aprobó ayer.
- Prefijo de TODAS las funciones globales y constantes: csg / CSG_. Ninguna función puede repetir el nombre de otra de la casa (los <script> de index.html comparten ámbito). El bucle que lo comprueba y que tiene que devolver VACÍO:
  for f in $(grep -oE 'src="js/[^"]+"' index.html | sed 's/src="//;s/"$//'); do grep -oE "^(async )?function [A-Za-z_$][A-Za-z0-9_$]*" "$f" | sed -E 's/^(async )?function //' | sort -u | sed "s|$| $f|"; done | awk '{n[$1]++; f[$1]=f[$1]" "$2} END {for (k in n) if (n[k]>1) print k":"f[k]}'
- Ayudas de la casa que YA existen y se usan tal cual (no se redefinen): switchView(id) en js/app.js; toast(msg) en js/app.js; verificarSesion() en js/auth.js (devuelve un objeto con .user = identificador del miembro, o null); AUTH_MIEMBRO_KEY = 'faro.miembro' (localStorage, guarda solo el identificador del miembro); el cliente de Supabase es window.faroSb (único de la casa; en cuadernos se toma con rcuSb(), copia ese patrón como csgSb()); redMiembro() en js/tools/redaccion.js (verificarSesion().user o 'josue'); vozAbrirPegar(null) en js/tools/voz-prestada.js abre la hoja de pegar de La Voz Prestada; VOZ_ETIQUETAS ya entiende 'consigna' → campo encargo (y titulo/voz/maquina/genero); VOZ_GENEROS lleva los emojis 📖 🧭 🪶 🗞️ ✉️ 🎙️ 📰 📚 🎤 (📜 no está); VOZ_MAQUINAS = ['Gemini','Claude','ChatGPT','Perplexity']; rcuVivos() devuelve las fichas vivas de Cuadernos ({id, titulo, url, ...}) si ese archivo cargó.
- Marcado de la casa: la vista es <div id="view-X" class="view"> con <header class="app-header compact"> (un <button class="back-icon-btn"> con <i class="fa-solid fa-arrow-left">, un <h1 class="view-title">, y un botón de acción) y <div class="view-scroll">. Las hojas son <div id="…-overlay" class="fin-modal-overlay" style="display:none;"><div class="fin-modal"><div class="fin-modal-head"><div class="des-modal-title">…</div><button type="button" class="fin-modal-close" aria-label="Cerrar"><i class="fa-solid fa-xmark"></i></button></div>…</div></div> (z-index 100, hoja pegada abajo). El bloque de presentación de una vista vacía es .msug-intro (mira #view-voz en index.html). Tokens: --surface, --bg, --border, --text, --muted, --faint, --brand, --nav-h (68px), --r, --r-sm, --shadow, --shadow-md. NO existe --card.
- Cableado exacto en index.html: el <link rel="stylesheet" href="css/consigna.css"> va justo después de la línea de css/redaccion-cuadernos.css (línea 14); el botón del Acceso Rápido va justo después del botón data-qa="view-voz" y antes del de data-qa="view-antena"; las dos vistas nuevas (#view-consigna y #view-consigna-editor) se insertan justo ANTES de la línea <div id="view-redaccion" class="view">; las tres hojas (#csg-ver-overlay, #csg-usar-overlay, #csg-pegar-overlay) se insertan justo ANTES de <div id="rcu-overlay" class="fin-modal-overlay" …>; el <script src="js/tools/consigna.js"></script> va justo después de <script src="js/tools/voz-adjunto.js"></script> y antes de <script src="js/app.js"></script>.
- En js/app.js, dentro de switchView: añadir «if (id === 'view-consigna' && typeof initConsigna === 'function') initConsigna();» al lado de la línea de view-voz, y añadir 'view-consigna-editor' a la condición que pone display:none al #destello-fab (la que ya lista view-chat, view-redaccion-editor y view-redaccion-pieza). En sw.js, CACHE_NAME pasa de 'faro-app-v127' a 'faro-app-v128'.
- La dirección del Acceso Rápido en app.js ya llama switchView(btn.dataset.qa) para cualquier data-qa="view-…": no hay que tocar nada más.
- El servidor estático YA corre en http://localhost:8124 (si no responde, levántalo con «node _dev/servidor-estatico.js &» desde la raíz del repositorio). Las sondas se corren sin cabeza con: NODE_PATH=/opt/node22/lib/node_modules SONDA_MS=600000 node ${RUNNER} _dev/probe-consigna.html  (imprime cada comprobación y RESULTADO: APRUEBA/SUSPENDE, más los errores de página).
- PostgreSQL local YA corre: createdb -h /tmp/pg -p 55432 -U postgres consignatest ; psql -h /tmp/pg -p 55432 -U postgres -v ON_ERROR_STOP=1 -d consignatest -f _dev/prueba-consigna-sql.sql (para repetir: dropdb -h /tmp/pg -p 55432 -U postgres consignatest primero).
- Estilo de la casa: código y comentarios en español, y cada comentario explica POR QUÉ está así (no qué hace la línea); nada de lo escrito por una persona llega a un atributo ni a innerHTML (createElement + textContent; direcciones solo http/https comprobadas con new URL() y puestas con setAttribute; enlaces target=_blank rel="noopener noreferrer"); las listas cerradas viven en el JS; letra de 16 px en todo recuadro que se enfoque; chips en una fila que se desliza; renglones de 44 px; el plegado con [hidden] + display:none !important en la regla propia; un tono por grupo; la cuenta dicha una vez; sin confirm(); retirar con lápida y dos toques.
- Plantillas que hay que LEER antes de escribir (con Read): js/tools/redaccion-cuadernos.js (local-first, csgConReloj ← rcuConReloj, bajar/subir/pendientes/fusionar, hojas, lista, rcuEl/rcuBtn, rcuTono), css/redaccion-cuadernos.css (tonos, barra, cuadrícula, ficha, hoja vertical), _dev/probe-redaccion-cuadernos.html (el arnés con la base de mentira a nivel de fetch), supabase/sql/redaccion_cuadernos.sql y supabase/sql/redaccion_cuadernos_comprueba.sql, _dev/prueba-redaccion-cuadernos-sql.sql, _dev/test-corrector-node.js (cómo se corre un archivo de la casa en Node con dobles de document/window/localStorage), y de js/tools/voz-prestada.js solo initVozPrestada y su DOMContentLoaded (líneas 1908-1940 y 11001-11040).
`

const NUCLEO_SCHEMA = { type: 'object', required: ['resumen', 'funciones', 'pruebas', 'dudas'], properties: {
  resumen: { type: 'string' }, funciones: { type: 'array', items: { type: 'string' }, description: 'una por función o constante pública: nombre(firma) — qué devuelve' },
  pruebas: { type: 'string', description: 'qué corre _dev/test-consigna-node.js y su resultado' }, dudas: { type: 'array', items: { type: 'string' } } } }
const SQL_SCHEMA = { type: 'object', required: ['resultado', 'archivos', 'notas'], properties: { resultado: { type: 'string' }, archivos: { type: 'array', items: { type: 'string' } }, notas: { type: 'string' } } }
const PANTALLA_SCHEMA = { type: 'object', required: ['resumen', 'archivos', 'humo', 'dudas'], properties: { resumen: { type: 'string' }, archivos: { type: 'array', items: { type: 'string' } }, humo: { type: 'string', description: 'qué se comprobó en el navegador sin cabeza y qué salió' }, dudas: { type: 'array', items: { type: 'string' } } } }
const SONDA_SCHEMA = { type: 'object', required: ['veredicto', 'secciones', 'arreglos', 'pendientes'], properties: { veredicto: { type: 'string' }, secciones: { type: 'array', items: { type: 'string' } }, arreglos: { type: 'array', items: { type: 'string' }, description: 'cada arreglo hecho en el código de la herramienta, con archivo y por qué' }, pendientes: { type: 'array', items: { type: 'string' } } } }
const DOCS_SCHEMA = { type: 'object', required: ['resumen'], properties: { resumen: { type: 'string' } } }
const REVIEW_SCHEMA = { type: 'object', required: ['hallazgos', 'resumen'], properties: { hallazgos: { type: 'array', items: { type: 'object', required: ['archivo', 'linea', 'gravedad', 'que', 'arreglo'], properties: { archivo: { type: 'string' }, linea: { type: 'number' }, gravedad: { type: 'string', enum: ['para', 'avisa'] }, que: { type: 'string' }, arreglo: { type: 'string' } } } }, resumen: { type: 'string' } } }
const FIX_SCHEMA = { type: 'object', required: ['arreglados', 'descartados', 'sonda', 'node', 'sql', 'repetidas', 'resumen'], properties: { arreglados: { type: 'array', items: { type: 'string' } }, descartados: { type: 'array', items: { type: 'string' }, description: 'hallazgos que no eran reales o que no se tocan, con el porqué' }, sonda: { type: 'string' }, node: { type: 'string' }, sql: { type: 'string' }, repetidas: { type: 'string' }, resumen: { type: 'string' } } }

phase('Núcleo')
log('Núcleo puro (vocabulario, moldes, armado, lector, repaso, nube) y SQL, en paralelo')
const [nucleo, sql] = await parallel([
  () => agent(`Eres quien escribe el NÚCLEO de La Consigna, la herramienta nueva de F.A.R.O. ${HECHOS}

TU TRABAJO (solo estos archivos): escribir js/tools/consigna.js — PARTE 1, el núcleo sin pantalla — y _dev/test-consigna-node.js.

En js/tools/consigna.js, en este orden y con un comentario de cabecera largo en español que diga qué es la herramienta, el espejo con La Voz Prestada, y las reglas de la §10 con su porqué (como la cabecera de redaccion-cuadernos.js):
1. Las constantes de la especificación, COMPLETAS y con el texto literal de la §3 (no resumas, no inventes, no recortes frases): CSG_EMOJI, CSG_CLASES (con emoji, nombre, «para qué» de la baldosa, molde recomendado), CSG_BLOQUES (el vocabulario de la §3.0 con rotulo, para_que, ejemplo, frases, lista, sistema, plantilla, inicial), CSG_MOLDES (los 17 moldes con clase, nombre, cuando, recomendado, bloques [{id, ob, y las sobreescrituras que la §3 marca}], forma, sinEncabezados, rotuloMayus, cabecera, reordena, auto, pseudo, cierre, orquestar, vozPrestada), CSG_EQUIVALE, CSG_SINONIMOS (con .bucle para los patrones de la §5 paso 6), CSG_MAQUINAS (§4), CSG_TOPES (§9), CSG_PREFILL_MAX, CSG_TEXTOS (los textos de orquestar de la §7), CSG_COLUMNAS, las claves de localStorage de la §9.
2. Las funciones puras, sin DOM: csgEl/csgBtn (como rcuEl/rcuBtn, pueden ir aquí), csgClave, csgSlug, csgSlugId, csgClaveEtiqueta, csgBloqueDef(moldeId, id), csgVariables(texto), csgRellena(t, pieza, maquina, valores), csgCuerpo, csgArmar(pieza, maquinaId, valores, opciones) → {principal, sistema, encargo, skill, mermaid, plan} EXACTAMENTE como la §7 (los ejemplos de la §7 son la verdad carácter por carácter), csgMmdId, csgMmdTexto, csgLeer(texto) → {clase, molde, titulo, bloques, material, variables, avisos} como la §5 pasos 0-9, csgProponerMolde, csgRevisar(pieza, maquinaId) → {para, avisa, palabras, tokens, cabe} como la §5 paso 3, csgPodar(p) (§9), csgFusiona(local, nube) (§9, unión por uid/vid), csgNuevoId, csgHoy, csgExportarTexto(lista) (§7 al final), csgApuntarUso (la parte de datos), csgNuevaVersion.
3. La nube, copiando el patrón de redaccion-cuadernos.js: csgSb, csgAutor (§9: verificarSesion().user → localStorage faro.miembro → ''), csgConReloj (8 s, código FARO_RELOJ), csgLeeLocal/csgGuardaLocal (faro_consigna_v1, lápidas seis meses), csgDesdeFila/csgAFila, csgBajar (42P01 → sin-tabla; 42703 → quita la columna que nombra el error y repite; lo demás → sin-senal), csgMotivoDe, csgSubir (upsert por id con onConflict 'id'; una pieza sin autor NO se sube y queda con motivo sin-sesion; una con noCabe tampoco), csgSubirPendientes (UN viaje), csgPersistir, csgSubirLuego (respiro de dos segundos), y el estado (_csgLista, _csgNube, etc.).
4. NADA de DOM a nivel de archivo: el archivo tiene que poder cargarse en Node con dobles de document/window/localStorage (mira _dev/test-corrector-node.js). Las funciones de pantalla (compositor, anaquel, hojas, initConsigna, DOMContentLoaded) las escribe OTRO agente después de ti, al final del mismo archivo: deja al final un comentario «/* ── PANTALLA: la escribe la parte 2 ── */» y NO las escribas tú. Pero deja definidas las firmas que la pantalla va a necesitar en tu resumen.
5. _dev/test-consigna-node.js: carga js/tools/consigna.js en Node (vm o require con dobles) y comprueba, imprimiendo ✔/✘ y terminando con «RESULTADO: APRUEBA/SUSPENDE» y código de salida 1 si suspende: (a) los 17 moldes existen, cada bloque de cada molde resuelve con csgBloqueDef a un rótulo, un para qué, un ejemplo y AL MENOS CUATRO frases; (b) el Encargo completo de la §7 armado en xml, md y seguida es IGUAL carácter por carácter a los tres textos de la §7 (cópialos literales en la prueba); (c) el SKILL.md de la §7, el Mermaid de la Cadena y el de la estrella, el plan del bucle y los auto-bucles de la §7 salen iguales a los ejemplos; (d) sistema + encargo == principal en xml; (e) csgLeer con los casos de la sonda 7 de la §11 (CO-STAR en XML → seis bloques; frontmatter → habilidad skill; tres pares «a → b» en un prompt Con ejemplos → NUNCA grafo; «Investigador → Guionista» y otra arista sin bloque ejemplos → grafo cadena; parada+tope+paso sin bloques de prompt → bucle hasta; «## Tarea … Repite esto para cada tema» → prompt encargo con aviso «parece que se repite»; siete renglones de prosa que empiezan por palabras de bloque sin dos puntos → UN bloque tarea; palabras de entrada == palabras de salida; lo no entendido nombrado con su renglón); (f) csgRevisar: obligatorio vacío para, {{}} para, bucle sin tope para, arista a nodo inexistente para, name de SKILL.md con mayúscula para, description de 1025 para, dos pares de menos para; teléfono/correo avisa; Perplexity sin dirección avisa; NotebookLM con <etiquetas> avisa; (g) csgPodar con 250 usos → 200 y 11 versiones de 30.000 → menos de 200.000 con las viejas plegadas; (h) csgFusiona: unión por uid, ok gana a null, escalares por actualizado; (i) csgVariables y csgRellena con {{maquina}}, {{titulo}}, {{hoy}}. Corre la prueba hasta que APRUEBE, y comprueba que «node --check js/tools/consigna.js» pasa.
Devuelve el resumen con TODAS las funciones y constantes públicas (nombre, firma, qué devuelve): es lo único que verá quien escriba la pantalla.`, { label: 'núcleo', phase: 'Núcleo', schema: NUCLEO_SCHEMA, effort: 'max' }),
  () => agent(`Eres quien escribe el SQL de La Consigna, la herramienta nueva de F.A.R.O. ${HECHOS}

TU TRABAJO (solo estos archivos): supabase/sql/consigna.sql, supabase/sql/consigna_comprueba.sql y _dev/prueba-consigna-sql.sql, según la §9 y la §11 (final) de la especificación, calcados de los tres archivos vecinos de Cuadernos (léelos enteros: la cabecera con el PARA QUÉ y los POR QUÉ, la guardia do $guardia$ que nombra seguridad_familia_1_puerta.sql, la tabla con sus checks y sus comentarios, el índice, el disparador touch con set search_path, la RLS de la casa con es_familia() sin política de delete, el revoke a anon y a authenticated con el grant de select/insert/update, y la comprobación EN VERTICAL al final con una fila por cosa: tabla, columnas 21, políticas 3, RLS true, disparador 1, delete 0, puerta anon 0). Los checks llevan EXACTAMENTE los números de CSG_TOPES de la §9 (bloques 60000, material 20000, estantes 1000, notas 4000, bitacora 60000, versiones 200000; titulo btrim 1-200; id 4-60; clase 1-20; molde 1-30; maquina 40; cuaderno 80; autor 40; version 1-100000; usos >= 0). consigna_comprueba.sql solo mira (con query_to_xml para contar filas de una tabla que puede no existir, como criba_comprueba.sql). La prueba _dev/prueba-consigna-sql.sql: el Supabase mínimo (roles anon y authenticated, grant usage on schema public, create schema auth, GRANT USAGE ON SCHEMA AUTH to anon, authenticated, auth.users, auth.uid(), familia_miembros y es_familia()), la guardia que PARA nombrando el archivo, el archivo corrido DOS veces sin duplicar políticas ni disparador, los checks que muerden (sin título, clase de 21, bloques que no es lista, bitacora de 60.001, versiones de 200.001, material de 20.001, id de dos letras, version 0), el disparador que pone la hora, y la puerta DOS VECES Y EN ESTE ORDEN: primero que el revoke le quitó a anon el permiso de tabla y que authenticated no tiene delete; después grant all como Supabase para probar que la RLS sigue dejando fuera a anon y a un usuario con sesión que no es de la casa, y que uno de la casa lee, inserta y corrige; sin ninguna security definer; y termina con «RESULTADO: APRUEBA». Córrela contra el PostgreSQL local hasta que apruebe (dropdb entre intentos). Y comprueba que el archivo principal pasa con psql -v ON_ERROR_STOP=1 --single-transaction sobre una base limpia que ya tenga es_familia(), que es como lo corre el editor de Supabase.`, { label: 'sql', phase: 'Núcleo', schema: SQL_SCHEMA, effort: 'high' }),
])
if (!nucleo) throw new Error('el núcleo no volvió')
log(`Núcleo: ${nucleo.funciones.length} funciones públicas · pruebas: ${nucleo.pruebas.slice(0, 120)}`)
if (sql) log(`SQL: ${sql.resultado.slice(0, 160)}`)

phase('Pantalla')
const pantalla = await agent(`Eres quien escribe la PANTALLA de La Consigna, la herramienta nueva de F.A.R.O. ${HECHOS}

El núcleo YA está escrito en js/tools/consigna.js (parte 1) por otro agente; léelo entero antes de empezar y NO lo reescribas: añade la parte 2 al final, después del comentario «/* ── PANTALLA ── */». Este es el resumen de su API:
${nucleo.resumen}
FUNCIONES Y CONSTANTES PÚBLICAS DEL NÚCLEO:
${nucleo.funciones.join('\n')}
${nucleo.dudas.length ? 'DUDAS QUE DEJÓ EL NÚCLEO (resuélvelas tú, en su código si hace falta, y dilo):\n' + nucleo.dudas.join('\n') : ''}

TU TRABAJO: (1) la parte 2 de js/tools/consigna.js: el anaquel (§8: cabecera, barra, buscador con el único mando, chips de clase con su cuenta, filtros puestos con su equis y la cuenta «3 de 12», el renglón de la bitácora preguntada de §6.7, los grupos plegables con display calculado y lo abierto recordado con el eje delante, las fichas en cuadrícula ▦ y la lista ☰, el menú ⋯ con Usar · Copiar · Corregir · Duplicar en otro molde… · Versiones · Bitácora · Estantes · 📓 Ligar al cuaderno · Retirar con dos toques, las retiradas en un <details>, la franja de la nube solo con algo que arreglar y el renglón discreto al pie si va bien, la vista vacía); el compositor como VISTA #view-consigna-editor (§5: la hoja de elegir clase con las cuatro baldosas y «📋 Pegar lo que ya tengo», la hoja de pegar y repartir con la propuesta en chip, la cabecera con el título propuesto sin pisar (_csgAuto), los chips de molde con la hoja vertical y «❓ ¿Cuál elijo?», los chips de máquina y de estante, los bloques como tarjetas con rótulo y punto, para qué (plegable con «Sin explicaciones»), recuadro que crece de 16 px con el ejemplo de placeholder y «📄 usar el ejemplo», los inicial puestos, las frases hechas como chips deslizables que meten y quitan con setRangeText e input a mano y pointerdown cancelado, «{ } Variable», «＋ Otro» en los lista, la fila de variables, el contador, 📎 Material plegado con sus reglas, la barra inferior FIJA con [👁 Vista previa] [💾 Guardar] [▶ Usar], el borrador en cada tecla en faro_consigna_borrador_v1 y su recuperación); la vista previa (§5 paso 3, con los chips de máquina, el <pre class="csg-pre"> con pre-wrap y 16 px, las pestañas del grafo y del bucle, y el REPASO con lo que PARA a la vista y lo que avisa plegado, cada aviso con su chip de arreglo); la hoja de usar (§5 paso 4: variables con último valor por pieza y los tres últimos como chips, el texto que se rellena en vivo, el Material plegado, el pie [📋 Copiar] [📤 Compartir] [↗ Abrir X] que copia PRIMERO y abre con window.open(url, '_blank', 'noopener,noreferrer') con el ?q= solo si cabe en 1.500 codificados, «Copiar sistema/encargo», «⬇ Descargar SKILL.md», «⬇ grafo.mmd», «Copiar con huecos», csgApuntarUso, y «📖 Guardar lo que devolvió» solo en los moldes vozPrestada, que hace switchView('view-voz') y vozAbrirPegar(null)); guardar (§5 paso 5) con la versión n+1 y el aviso de título repetido; versiones y bitácora en sus hojas; initConsigna() con el patrón de initVozPrestada (pinta YA con lo del aparato, baja, fusiona, sube pendientes, repinta) y el DOMContentLoaded que engancha los botones fijos del HTML. (2) css/consigna.css calcado de css/redaccion-cuadernos.css: tonos por clase y csgTono, la barra con baldosas, la cuadrícula con align-items:start, la ficha, la lista, la hoja vertical de 44 px, los grupos plegables con .csg-grupo[hidden]{display:none !important}, el compositor (tarjetas de bloque, chips, la barra fija con safe-area y tres botones que caben en 320 px), el .csg-pre, la hoja de usar; hover solo dentro de @media (hover: hover); NUNCA var(--card). (3) El cableado exacto de index.html, js/app.js y sw.js que dicen los HECHOS: el botón del Acceso Rápido (class qa-icon qa-csg, icono fa-solid fa-scroll, rótulo «La Consigna»), la regla .qa-csg con el degradado #3f6212 → #a3e635 (en css/consigna.css), las dos vistas, las tres hojas, el link y el script.
Cuando termines: «node --check js/tools/consigna.js»; el bucle de funciones repetidas (tiene que devolver vacío); «node _dev/test-consigna-node.js» sigue aprobando; y una prueba de humo con Playwright sin cabeza (NODE_PATH=/opt/node22/lib/node_modules; mira ${RUNNER} para el patrón: abre http://localhost:8124/index.html, esconde #login-screen, enseña #app-container, switchView('view-consigna'), pulsa ＋ Nueva, elige Prompt, escribe una Tarea, guarda, vuelve al anaquel, usa la pieza, y comprueba que no hubo ningún pageerror y que la ficha está; con fetch de mentira que devuelva 42P01 a /rest/v1/consigna_piezas y '[]' al resto). Arregla lo que falle. Devuelve el resumen, los archivos tocados, lo que la prueba de humo comprobó y las dudas.`, { label: 'pantalla', phase: 'Pantalla', schema: PANTALLA_SCHEMA, effort: 'max' })
if (!pantalla) throw new Error('la pantalla no volvió')
log(`Pantalla: ${pantalla.archivos.join(', ')} · humo: ${pantalla.humo.slice(0, 160)}`)

phase('Sonda y documentación')
const [sonda, docs] = await parallel([
  () => agent(`Eres quien escribe la SONDA de La Consigna, la herramienta nueva de F.A.R.O, y quien la lleva hasta APRUEBA. ${HECHOS}

La herramienta ya está escrita: js/tools/consigna.js, css/consigna.css, y su cableado en index.html, js/app.js y sw.js; la prueba de Node _dev/test-consigna-node.js aprueba; el SQL está en supabase/sql/consigna.sql. Léelo todo antes de escribir.
Resumen de la pantalla: ${pantalla.resumen}
${pantalla.dudas.length ? 'Dudas que dejó la pantalla: ' + pantalla.dudas.join(' | ') : ''}

TU TRABAJO: escribir _dev/probe-consigna.html con TODAS las comprobaciones de la §11 de la especificación (las 22, con el recorte de que «☑ Elegir» no existe: en la 18 se mide el último botón de la barra), calcada del arnés de _dev/probe-redaccion-cuadernos.html: un solo marco de 390 px que se ensancha o estrecha con iframe.style.width cuando mide (320 y 1000), la base de mentira a nivel de fetch (42P01 en modo sin tabla; 42703 ante una columna que NO esté en la lista de columnas leída del propio supabase/sql/consigna.sql, que la sonda trae con fetch y parsea; 201 guardando el cuerpo en modo puesta; 23502 si falta autor), w.open apuntado, portapapeles apuntado, confirm contado, verificarSesion de mentira que se puede poner en null, y el mismo marco al cambiar de base (nunca un segundo index.html). Los botones SE PULSAN (click de verdad sobre el nodo, y elementFromPoint para las hojas), los gestos con eventos de puntero, los colores y el display se miran CALCULADOS, las medidas en píxeles con getBoundingClientRect. Cada comprobación con ok(condición, 'qué está bien', 'qué salió mal con el dato'). Termina poniendo APRUEBA o SUSPENDE delante en document.title y «RESULTADO: …» en el <pre>.
Córrela sin cabeza con el runner de los HECHOS hasta que APRUEBE. Cuando una comprobación suspenda, decide con honestidad si la sonda está mal escrita o si la herramienta tiene un defecto: si es la herramienta, ARREGLA el defecto en js/tools/consigna.js, css/consigna.css o index.html (respetando la especificación y los HECHOS) y apúntalo en «arreglos» con el archivo y el porqué; no aflojes nunca una comprobación para que apruebe. Vuelve a correr también node _dev/test-consigna-node.js y el bucle de funciones repetidas después de cada arreglo. Devuelve el veredicto, la lista de secciones con su estado, los arreglos hechos en el código y lo que quede pendiente.`, { label: 'sonda', phase: 'Sonda y documentación', schema: SONDA_SCHEMA, effort: 'max' }),
  () => agent(`Eres quien DOCUMENTA La Consigna, la herramienta nueva de F.A.R.O, en los archivos de la casa. ${HECHOS}

La herramienta ya está escrita (js/tools/consigna.js, css/consigna.css, index.html, js/app.js, sw.js, supabase/sql/consigna.sql, consigna_comprueba.sql, _dev/prueba-consigna-sql.sql); léela para escribir con verdad, no de memoria. Resumen de la pantalla: ${pantalla.resumen}

TU TRABAJO (solo estos archivos): (1) En CLAUDE.md, una sección nueva «## Normativa: las indicaciones a las máquinas se escriben en La Consigna», insertada justo ANTES de «## Normativa: los textos de encargo se leen en La Voz Prestada», con el mismo tono y forma que las secciones vecinas (la de los cuadernos es el modelo: el pedido del autor con su fecha (22 de septiembre de 2026) y sus palabras textuales «me facilites los mejores formatos tanto para realizar prompt, habilidades skills, grafo o loop en cuanto a las indicaciones que haga a diferentes inteligencias artificiales. Tenerlas inventariadas, debes crear toda una lógica de proceso que me facilite redactarlos con indicaciones programadas»; dónde vive; qué resuelve; que es el ESPEJO de La Voz Prestada y NO es Redacción ni Redes; el vocabulario en cinco líneas; «Veintidós reglas, y ninguna es de adorno» con las 22 reglas de la §10 de la especificación escritas con su porqué (no como lista seca); el párrafo «Antes de publicar un cambio de La Consigna» con los tres comandos (servidor, sonda, prueba de Node) y el del SQL contra PostgreSQL; y dos párrafos sobre qué mira la sonda y qué mira la prueba del SQL, ordenados por gravedad, en el estilo de los vecinos). (2) En la tabla de «## Dos paletas», añadir consigna.css a la fila de hojas de la aplicación. (3) En PLAN-FARO-PRIVADO.md, en la lista de SQL que falta correr, una entrada para supabase/sql/consigna.sql en el mismo formato que la de redaccion_cuadernos.sql (léela y cópiala). No toques nada más. Devuelve un resumen de lo escrito.`, { label: 'documentación', phase: 'Sonda y documentación', schema: DOCS_SCHEMA, effort: 'high' }),
])
if (!sonda) throw new Error('la sonda no volvió')
log(`Sonda: ${sonda.veredicto} · arreglos en código: ${sonda.arreglos.length} · pendientes: ${sonda.pendientes.length}`)
if (docs) log(`Documentación: ${docs.resumen.slice(0, 160)}`)

phase('Revisión')
const LENTES = [
  { key: 'seguridad', lente: 'SEGURIDAD: nada de lo escrito por una persona llega a un atributo ni a innerHTML (busca cada innerHTML, insertAdjacentHTML, setAttribute con datos, href/src armados con texto; el <pre> con textContent); direcciones solo http/https comprobadas con new URL(); window.open con noopener,noreferrer y la dirección base LITERAL con encodeURIComponent y el tope de 1.500; el cuaderno ligado comprobado con URL(); el SQL con sus checks de forma y tope, RLS con es_familia() en las tres políticas, sin delete, revoke a anon, sin security definer; ningún dato de la casa (teléfonos, nombres) escrito en el código; la descarga del SKILL.md por Blob sin inyectar nada.' },
  { key: 'casa', lente: 'REGLAS DE LA CASA (CLAUDE.md): una sola fila de botones que se desliza con los botones primero; renglones de 44 px; letra de 16 px en todo recuadro y en el <pre> con pre-wrap; el plegado con [hidden] + display:none !important en la regla propia y mirado calculado; un tono por clase y csgTono para estantes; NUNCA var(--card) ni ningún token que no exista; la cuenta dicha una sola vez; la nube solo es franja con algo que arreglar; nada se abre solo; buscando se abre todo y no se guarda; retirar con lápida y dos toques sin confirm(); los chips insertan con setRangeText e input a mano y pointerdown cancelado; comentarios en español con el porqué; el prefijo csg en todo; el bucle de funciones repetidas vacío; sw.js sellado a v128; el compositor a 390 px con la barra fija que cabe en 320; antes de la primera ficha, como máximo barra, buscador, chips, filtros puestos y el renglón de la bitácora.' },
  { key: 'nube', lente: 'DATOS Y NUBE: local primero (faro_consigna_v1) y nube después; 42P01 = falta correr consigna.sql, 42703 = la base va vieja (se quita la columna que nombra el error y se sigue), FARO_RELOJ y todo lo demás = la señal, y ninguna de esas ramas vacía la lista; upsert por id con onConflict; csgSubirPendientes en UN viaje al abrir y al online; ninguna fila viaja sin autor (la pieza sin firma espera); lápidas de seis meses que ganan al arrancar; fusión de escalares por actualizado y de bitacora/versiones por UNIÓN de uid/vid con ok ganando a null; poda por bytes ANTES de guardar y de subir (bitácora 200 y 58.000; versiones 10 con bloques y 198.000; bloques nunca recortados: noCabe); material de más de 20.000 que no viaja; usos nunca menor que el largo de la bitácora; CSG_TOPES igual a los checks del SQL; csgAutor con sus tres fuentes.' },
  { key: 'espec', lente: 'CONFORMIDAD CON LA ESPECIFICACIÓN (léela entera en la ruta de los HECHOS): los 17 moldes con TODOS sus bloques, rótulos, para qués, ejemplos y al menos cuatro frases cada uno, con las sobreescrituras marcadas en la §3; csgArmar igual carácter por carácter a los ejemplos de la §7 (xml, md, seguida, SKILL.md, los dos Mermaid, el plan y los auto-bucles); csgLeer con los nueve pasos de la §5 y CSG_SINONIMOS completo; csgRevisar con todo lo que PARA y todo lo que AVISA de la §5 paso 3; la bitácora preguntada exactamente como la §6.7; las versiones como la §6.8; Duplicar en otro molde con CSG_EQUIVALE como la §6.9; la hoja de usar como la §5 paso 4; el anaquel como la §8 (sin ☑ Elegir, que es el recorte); el export como la §7; lo que NO hace de la §12 no está hecho.' },
  { key: 'sonda', lente: 'LA SONDA Y LAS PRUEBAS: _dev/probe-consigna.html cubre de verdad las 22 comprobaciones de la §11 (recorrelas una por una y di cuál falta o está aflojada); no hay dobles complacientes (la base de mentira exige autor, rebota columnas que no están en el SQL leído de verdad, y 42P01/42703 se distinguen); los botones se pulsan y no se llama a la función por dentro donde la §11 exige pulsar; los colores y el display se miran calculados; las medidas van en píxeles; el mismo marco al cambiar la base; _dev/test-consigna-node.js compara carácter por carácter contra los textos de la §7 copiados literales; _dev/prueba-consigna-sql.sql mira la puerta dos veces y en el orden bueno y lleva el grant usage on schema auth; y tanto la sonda como las pruebas APRUEBAN de verdad al correrlas tú mismo ahora.' },
]
const revisiones = (await parallel(LENTES.map(l => () => agent(`Eres un revisor ADVERSARIAL de La Consigna, la herramienta nueva de F.A.R.O, con una sola lente: ${l.lente}
${HECHOS}
Lee el código de verdad (js/tools/consigna.js, css/consigna.css, los trozos nuevos de index.html, js/app.js y sw.js, supabase/sql/consigna.sql, consigna_comprueba.sql, _dev/prueba-consigna-sql.sql, _dev/probe-consigna.html, _dev/test-consigna-node.js) y busca DEFECTOS REALES bajo tu lente, con archivo y línea. Corre lo que haga falta para comprobarlo (node --check, la prueba de Node, la sonda con el runner, el SQL con psql, el bucle de funciones repetidas). Cada hallazgo: archivo, línea, gravedad («para» si rompe una regla no negociable, una promesa de la especificación o el uso; «avisa» si es mejorable), qué pasa exactamente, y el arreglo concreto. No inventes hallazgos por rellenar: si bajo tu lente está bien, dilo con la lista vacía. No cambies ningún archivo.`, { label: `revisión:${l.key}`, phase: 'Revisión', schema: REVIEW_SCHEMA, effort: 'high' })))).filter(Boolean)
const hallazgos = revisiones.flatMap((r, i) => r.hallazgos.map(h => ({ lente: LENTES[i] ? LENTES[i].key : '?', ...h })))
log(`Revisión: ${hallazgos.length} hallazgos (${hallazgos.filter(h => h.gravedad === 'para').length} que paran)`)

phase('Arreglo')
const arreglo = await agent(`Eres quien CIERRA los hallazgos de la revisión de La Consigna, la herramienta nueva de F.A.R.O. ${HECHOS}

HALLAZGOS DE CINCO REVISORES (JSON): ${JSON.stringify(hallazgos, null, 1)}
RESÚMENES: ${revisiones.map((r, i) => (LENTES[i] ? LENTES[i].key : '?') + ': ' + r.resumen).join('\n')}

TU TRABAJO: comprueba cada hallazgo EN EL CÓDIGO antes de tocar nada (un revisor puede equivocarse: si no es real, lo descartas con el porqué); arregla TODOS los reales que paran y los que avisan que sean claros y baratos, respetando la especificación, los HECHOS y las reglas de la casa; si un arreglo cambia algo que la sonda o la prueba de Node miran, ajústalas para que sigan mirando la verdad (nunca las aflojes). Al final corre TODO y devuelve los resultados literales: node --check js/tools/consigna.js; node _dev/test-consigna-node.js; la sonda con el runner de los HECHOS (RESULTADO y los errores de página); el SQL (dropdb + createdb + psql de _dev/prueba-consigna-sql.sql); y el bucle de funciones repetidas (tiene que salir vacío). Si algo no aprueba, sigue arreglando hasta que apruebe o di exactamente qué queda y por qué.`, { label: 'arreglo', phase: 'Arreglo', schema: FIX_SCHEMA, effort: 'max' })

return {
  nucleo: nucleo.resumen, nucleo_dudas: nucleo.dudas,
  sql: sql ? sql.resultado : 'sin resultado',
  pantalla: pantalla.resumen, pantalla_humo: pantalla.humo, pantalla_dudas: pantalla.dudas,
  sonda: sonda.veredicto, sonda_secciones: sonda.secciones, sonda_arreglos: sonda.arreglos, sonda_pendientes: sonda.pendientes,
  docs: docs ? docs.resumen : 'sin resultado',
  hallazgos,
  arreglo,
}